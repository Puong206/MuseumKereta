<UserControl x:Class="MuseumApp.InputDialogBarang"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MuseumApp"
             mc:Ignorable="d" 
             d:DesignHeight="480" d:DesignWidth="450"
             MinWidth="380" MaxWidth="500"
             Background="#FDFDFD" Padding="25,20,25,25">
    <UserControl.Effect>
        <DropShadowEffect ShadowDepth="1" BlurRadius="8" Color="#D0D0D0" Opacity="0.4"/>
    </UserControl.Effect>
    <UserControl.Resources>
        <Style x:Key="DialogLabelStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Plus Jakarta Sans Medium"/>
            <Setter Property="Foreground" Value="#2D2B70"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
        </Style>
        <Style x:Key="DialogTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="BorderBrush" Value="#FFABADB3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="3" SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" 
                                          HorizontalScrollBarVisibility="Hidden" 
                                          VerticalScrollBarVisibility="{TemplateBinding VerticalScrollBarVisibility}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF84A9FF" />
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect ShadowDepth="0" Color="#84A9FF" Opacity="0.5" BlurRadius="5"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.56"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="DialogButtonStyle" TargetType="Button">
            <Setter Property="FontFamily" Value="Plus Jakarta Sans Bold"/>
            <Setter Property="Width" Value="90"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PrimaryDialogButtonStyle" TargetType="Button" BasedOn="{StaticResource DialogButtonStyle}">
            <Setter Property="Background" Value="#FFF26924"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style x:Key="SecondaryDialogButtonStyle" TargetType="Button" BasedOn="{StaticResource DialogButtonStyle}">
            <Setter Property="Background" Value="#FFE0E0E0"/>
            <Setter Property="Foreground" Value="#2D2B70"/>
        </Style>
    </UserControl.Resources>

    <StackPanel Orientation="Vertical">
        <TextBlock Text="Detail Barang" 
                   HorizontalAlignment="Center"
                   Margin="0,0,0,25"
                   FontFamily="Plus Jakarta Sans Bold" FontSize="22" Foreground="#2D2B70"/>

        <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="350">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="ID Barang:" Style="{StaticResource DialogLabelStyle}" Margin="0,0,10,10"/>
                <TextBox x:Name="IDBarangTextBox" Grid.Row="0" Grid.Column="1" Style="{StaticResource DialogTextBoxStyle}" Margin="0,0,0,10"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Nama Barang:" Style="{StaticResource DialogLabelStyle}" Margin="0,0,10,10"/>
                <TextBox x:Name="NamaBarangTextBox" Grid.Row="1" Grid.Column="1" Style="{StaticResource DialogTextBoxStyle}" Margin="0,0,0,10"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="ID Koleksi:" Style="{StaticResource DialogLabelStyle}" Margin="0,0,10,10"/>
                <TextBox x:Name="IDKoleksiTextBox" Grid.Row="2" Grid.Column="1" Style="{StaticResource DialogTextBoxStyle}" Margin="0,0,0,10"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Tahun Pembuatan:" Style="{StaticResource DialogLabelStyle}" Margin="0,0,10,10"/>
                <TextBox x:Name="TahunPembuatanTextBox" Grid.Row="3" Grid.Column="1" Style="{StaticResource DialogTextBoxStyle}" Margin="0,0,0,10"/>

                <TextBlock Grid.Row="4" Grid.Column="0" Text="Asal Barang:" Style="{StaticResource DialogLabelStyle}" Margin="0,0,10,10"/>
                <TextBox x:Name="AsalBarangTextBox" Grid.Row="4" Grid.Column="1" Style="{StaticResource DialogTextBoxStyle}" Margin="0,0,0,10"/>

                <TextBlock Grid.Row="5" Grid.Column="0" Text="Deskripsi:" Style="{StaticResource DialogLabelStyle}" VerticalAlignment="Top" Margin="0,5,10,10"/>
                <TextBox x:Name="DeskripsiTextBox" Grid.Row="5" Grid.Column="1" Style="{StaticResource DialogTextBoxStyle}" 
                         MinHeight="80" TextWrapping="Wrap" AcceptsReturn="True" 
                         VerticalScrollBarVisibility="Auto" Margin="0,0,0,15"
                         VerticalContentAlignment="Top" Padding="8,5"/>
            </Grid>
        </ScrollViewer>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,25,0,0">
            <Button x:Name="SimpanButton" Content="Simpan" Style="{StaticResource PrimaryDialogButtonStyle}" Click="SimpanButton_Click"/>
            <Button x:Name="BatalButton" Content="Batal" Style="{StaticResource SecondaryDialogButtonStyle}" Click="BatalButton_Click"/>
        </StackPanel>
    </StackPanel>
</UserControl>